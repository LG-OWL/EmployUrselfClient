import React, {useState} from 'react';

export default function LoginPage(props){
    const [aktuellerWert, setAktuellerWert] = useState('');

    return(
        <h2>Das ist eine LoginPage!</h2>
    );
}